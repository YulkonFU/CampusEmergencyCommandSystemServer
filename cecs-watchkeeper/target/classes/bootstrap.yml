server:
  port: 8004

spring:
  application:
    name: cecs-watchkeeper
  cloud:
    nacos:
      discovery:
        server-addr: nat.ckxgzxa.top:33300
        username: rst
        password: rst
      config:
        server-addr: nat.ckxgzxa.top:33300
        file-extension: yaml
        group: DEFAULT_GROUP
        prefix: ${spring.application.name}
